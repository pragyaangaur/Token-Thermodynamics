# Enough to run repro.py, the analysis scripts and the figures.
# Nothing here downloads or runs a language model. For that see requirements-models.txt.
# Versions are pinned to the ones the results in FINDINGS.md were produced with.
numpy==2.5.0
scipy==1.18.0
scikit-learn==1.9.0
matplotlib==3.11.0
